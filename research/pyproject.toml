[project]
name = "huffman-archiver-research"
version = "0.1.0"
description = "Generate data, test and create plots"
requires-python = ">=3.12,<3.14"

dependencies = [
    "matplotlib",
    "requests",
]
